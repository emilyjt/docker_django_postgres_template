from django.apps import AppConfig


class CoreConfig(AppConfig):
    name = "django_template.core"
    verbose_name = "Main"  # change this to anything and the django admin will reflect what is here.
